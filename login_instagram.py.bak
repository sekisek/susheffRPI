import asyncio
from pathlib import Path
from playwright.async_api import async_playwright

BASE = Path.home() / "social-bot"
PROFILE_DIR = BASE / "profiles" / "instagram"

async def main():
    PROFILE_DIR.mkdir(parents=True, exist_ok=True)

    async with async_playwright() as p:
        context = await p.chromium.launch_persistent_context(
            user_data_dir=str(PROFILE_DIR),
            headless=False,
            viewport={"width": 1440, "height": 1000},
            args=["--start-maximized"],
        )

        page = await context.new_page()
        await page.goto("https://www.instagram.com/", wait_until="domcontentloaded")

        print("\nLog in manually to Instagram in the opened browser.")
        print("After login is complete and the home page is loaded, press Enter here in the terminal.\n")
        input()

        await context.close()

if __name__ == "__main__":
    asyncio.run(main())
